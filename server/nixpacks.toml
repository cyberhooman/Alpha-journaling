[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci --workspace=server"]

[phases.build]
cmds = ["npm run build --workspace=server"]

[start]
cmd = "npm run start --workspace=server"
